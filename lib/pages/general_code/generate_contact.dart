import 'package:flutter/material.dart';

class GenerateContact extends StatelessWidget {
  const GenerateContact({super.key});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
